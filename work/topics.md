# work/ — topics

One line per topic folder. `/kb` consults this when proposing where a new item belongs. Folders accrete naturally — don't pre-create them.

- `strategy/` — strategy frameworks, competitive analysis, market notes (example seed folder)
- `aa-inbox/` — drop zone for unprocessed captures (not a topic; processed and emptied by `/kb`)
