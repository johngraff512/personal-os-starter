# work/ — cross-references

Pointers to *external* knowledge surfaces this vault can consult but does not absorb. `/kb ask` reads this to decide where to look beyond the vault, and **asks before reading** from any external surface.

This is a stub — replace the examples with your own. Keep it to pointers, not content.

## External surfaces

- `<your-research-kb>/` — *(example)* a separate research/reference knowledge base. Describe what it contains and when to consult it.
- `<your-cloud-store>/` — *(example)* a cloud drive (OneDrive, Google Drive, Dropbox) holding source documents. If you index its metadata, point to your `course-archive-index.md` here.

## How `/kb ask` should use this

1. Answer from `work/index.md` + summaries first.
2. Only if external context is needed, read this file to identify the right surface.
3. Ask before reading from any external surface.
