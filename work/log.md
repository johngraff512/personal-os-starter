# work/ — ingestion log

Append-only record of every `/kb` ingestion. One entry per item (or batch). For PDF items, the reference setup also logs parser model + token usage.

## 2026-01-12
- **Ingested:** `strategy/2026-01-12 Example Author. A Note on Competitive Strategy` — article from URL drop. Generated `_text.md` + `_summary.md` (APA citation block). (Example seed entry — replace with your own.)
